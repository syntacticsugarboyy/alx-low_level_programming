#include "main.h"

/**
 * _abs - absolute
 * @n: integer
 * Description: A function that prints the absolute value of numbers
 *
 * Return: 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else
	{
		return (n);
	}
}
